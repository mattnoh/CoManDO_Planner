# scripts/

Development and profiling utilities.

- `apply_profile.sh` – Helper script that applies a performance profile to the compiled binaries (e.g., sets CPU affinity, tweaks scheduling, or loads a predefined set of parameters). Used during benchmarking and experiments.